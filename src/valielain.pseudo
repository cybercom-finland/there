## välieläin

d = new directory();
entries = d.search("root@pizz");

s = new storage();
c = s.list_clearances();

# create new clearance

c1 = "webtunkkaajat";
a1 = new algo("up/dn");
p1 = new password();
p1.change("tunkki");
s.store(c1, ".algo", "up/dn");
s.store(c1, "halli", a1.encrypt(p1.serialize());

#
c2 = "root";
a2 = new algo(s.retrieve(c2, ".algo"));
p2 = new password(a2.decrypt(s.retrieve(c2, "root@pizzalandia")));
print p2.latest();
print p2.history();

## 

